# Docker

[Docker Desktop](https://www.docker.com/products/docker-desktop/). Installed via the Brewfile cask.

## What `install.sh` does

Opens `Docker.app` if not already running, then prints `docker --version` and `docker compose version` as a sanity check.

## What gets loaded into your shell

- `aliases.zsh` — Docker / Docker Compose shortcuts.

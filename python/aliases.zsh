# Python aliases
# Note: If pyenv is active, it will manage python/pip commands via shims
# These aliases are fallbacks for when pyenv is not managing a specific version

# Use Homebrew's Python as fallback (Apple Silicon path)
alias python3='/opt/homebrew/bin/python3'
alias pip3='/opt/homebrew/bin/pip3'

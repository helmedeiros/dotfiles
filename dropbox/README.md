# Dropbox

[Dropbox](https://www.dropbox.com) desktop client. Installed via the Brewfile cask.

## What `install.sh` does

Opens `Dropbox.app` if not already running, so the agent starts syncing immediately after install.
